# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与
[语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-09-12

首个版本。在真实环境（ComfyUI 0.32.0 / 前端 1.48.7 / Python 3.13.12 /
PyTorch 2.10.0+cu130 / RTX 5060 Ti 16GB / Windows 11）上完成端到端验证。

### 新增

- **实时监控**：GPU 利用率、显存、温度、功耗、风扇、SM 频率；CPU 总体与逐核占用、
  频率；内存、可用内存、交换分区；磁盘读写速率。默认 1 秒采样，运行中自动提速到 500 ms。
- **三层 GPU 后端自动降级**：`pynvml` → `nvidia-smi` → `torch.cuda`，
  全部不可用时其余指标照常工作，面板不报错。
- **逐节点峰值归因**：基于 ComfyUI 官方 `ProgressRegistry` / `ProgressHandler`
  扩展点记录每个节点的执行窗口，并给出窗口内各指标最大值，标明每个运行级峰值
  发生在哪个节点。支持在面板中点击节点名定位到画布。
- **运行历史**：内存中保留最近 20 次运行，含时长、状态、峰值与逐节点明细。
- **报错捕获**：异常类型、消息、完整堆栈、出错节点及其输入摘要；
  失败时额外落盘一份带硬件时间线的详细快照。
- **持久化**：每次运行写入 `logs/*.json`，按数量滚动清理。
- **AI 诊断**：支持 DeepSeek 与任意 OpenAI 兼容接口，可对运行或具体报错做分析，
  支持追问。发送内容仅限报错、运行指标与节点列表，界面提供"查看将要发送的数据"预览。
- **零运行时依赖**：只用 ComfyUI 自带 `psutil` 与 Python 标准库。
- **可选节点** `System Monitor (Report)`：把指标接入工作流。

### 兼容性处理

- 节点级事件优先走 `ProgressRegistry`；该 API 不存在时自动回退到监听
  `executing` / `executed` websocket 消息。
- 所有 ComfyUI 钩子均包裹异常保护，插件自身出错不会中断用户的生成任务。
- 前端扩展使用相对导入 `../../scripts/app.js` 并带多重回退，
  样式表路径由 `import.meta.url` 推导，不写死 `/extensions/<folder>/`。

### 安全

- API Key 仅保存在本地 `sysmon_config.json`，已被 `.gitignore` 排除。
- 所有 HTTP 接口返回的是掩码视图（`sk-a...7890`），原始 Key 从不出站到浏览器。
- 掩码值写回配置时会被 `Config.update()` 拒绝，避免误覆盖真实 Key。
- 已加回归测试：断言 Key 不出现在任何配置视图、运行记录、报错载荷与日志文件中。

### 已验证

- 后端 127 项断言（含针对本地 mock 服务端的完整大模型请求/响应往返，
  以及 API Key 泄露回归测试）。
- HTTP 接口 57 项断言（真实 aiohttp 测试服务器 + 本地 mock 大模型端点）。
- 前端静态检查（语法、重复声明、相对导入、调用未定义的函数）。
- 真实运行验证：真实工作流的逐节点归因、故意触发的节点异常捕获、
  headless 浏览器中的面板渲染，并逐一实测全部 6 个标签页（0 异常）。
- 与用户实际安装的 16 个 custom_nodes 节点包同时加载，无冲突。

### 修复（开发期发现，均已在发布前解决）

- **AI 标签页的状态被轮询冲掉**（三处同源缺陷）：`renderAi()` 每次轮询都会重建整个
  面板，而分析结果、报错提示、上下文预览、追问输入框原先都是一次性 append 到 DOM 的，
  因此会在 1 秒内被清空。
  - 「继续追问」输入框永远不出现——`document.getElementById("sysmon-ai-output")`
    在重建后返回 `null`，追问结果无处可写；
  - 分析失败时的错误提示一闪而过，用户根本看不到失败原因；
  - 「查看将要发送的数据」预览同样看不见。
  现改为把 AI 内容存进组件状态（`aiChat` / `aiPanel` / `lastAnalysisError`）
  并在每次渲染时重建，三处一次性修好。已用真实 DOM 断言覆盖（22 项浏览器测试）。
- **面板关闭后无法找回**：悬浮胶囊按钮的回调误写成 `this.setCollapsed(false)`，
  而该方法不会清除 `is-hidden`，导致点过 ✕ 之后面板再也打不开。改为 `this.show()`，
  并在静态检查中加入「可见性契约」规则（含反向验证）。
- **设置标签页完全不可用**：`panel.js` 三次调用 `checkRow()` 却从未定义该函数，
  点击「设置」会抛 `ReferenceError`。由于该标签页只在点击时渲染，
  常规加载检查无法发现；已补上实现，并给静态检查加上
  「调用了未定义的函数」规则（含反向验证）。
- **节点 class_type 为空**：`_collect_titles()` 用 `node.get("type")` 兜底标题，
  而前端 workflow JSON 并不包含 `class_type`，导致标题与类型共用同一个字典、
  类型读出空字符串。现改为以 prompt 字典为 `class_type` 的权威来源，
  workflow JSON 只负责用户自定义标题；含子图节点与缺失节点的回归测试。
- **报错快照在运行结束前落盘**：原先在 `execution_error` 触发瞬间就写文件，
  此时运行尚未收尾，快照里没有时长、没有节点列表、硬件时间线为空
  （5 KB 的近乎无用文件）。现推迟到执行器的 `finally` 中、运行关闭之后再写，
  同一个文件变成 35 KB 且包含 120 个采样点，真正可用于诊断。
- **`Config.update()` 缺少掩码防护**：防护原先只写在 HTTP 路由里，
  任何其它调用方把界面上的掩码值（`sk-a...7890`）传回来都会静默覆盖真实 Key。
  现下沉到配置对象本身。
- **启动后一段时间内没有数据**：`start()` 只启动采样线程就返回，`latest()` 在
  第一个采样周期前一直是 `None`，面板显示「正在采集数据…」、上报节点返回 0。
  现在 `start()` 会先同步采集一次。
- **上报节点不显示工作流名**：`_format_run()` 输出里没有工作流名称，已补上。
- **AI 开关形同虚设**：「启用 AI 分析」原先只有前端提示、后端从不检查，
  关掉之后点分析依然会把数据发出去。现改为服务端强制（403），
  并测试「关闭时不得发出任何请求」。
- **追问线程无上限**：`_conversations` 以 `prompt_id` 为键且从不回收，
  长时间运行会持续增长。现改为有界 LRU。
- 前端扩展最初完全没有加载样式表（漏了 `<link>`），面板无样式且被
  ComfyUI 自身提示遮挡；样式表路径改由 `import.meta.url` 推导。
- 面板 z-index 由 1000 提升到 10001，避免被前端 toast 层（10000）遮挡。
